# Firebase Functions environment variables
# Copy to functions/.env for local emulator use.
#
# SECRETS (sensitive — never commit values):
#   Set via Secret Manager before deploying:
#     firebase functions:secrets:set STRIPE_SECRET_KEY
#     firebase functions:secrets:set STRIPE_WEBHOOK_SECRET
#   For emulator, create functions/.secret.local:
#     STRIPE_SECRET_KEY=sk_test_...
#     STRIPE_WEBHOOK_SECRET=whsec_...
#
# NON-SECRET STRIPE PRICE IDs (safe to commit per environment):
# Create these products in the Stripe dashboard, then paste the Price IDs below.
#
#   Parent plans:
STRIPE_PRICE_PARENT_MONTHLY=price_
STRIPE_PRICE_PARENT_YEARLY=price_
STRIPE_PRICE_PARENT_LIFETIME=price_
#
#   Teacher plans:
STRIPE_PRICE_TEACHER_MONTHLY=price_
STRIPE_PRICE_TEACHER_YEARLY=price_
STRIPE_PRICE_TEACHER_LIFETIME=price_
